import React, { createContext } from "react";

const Context = createContext(null);

/**
 * Log type list
 *
 * Deposit
 * Withdraw
 * New Acount
 * Log-in
 * Log-in attempt
 * Log-out
 */

export default Context;
